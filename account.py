import mysql.connector
x = mysql.connector.connect(host = 'localhost',
                               username = 'viraj',
                               password =  'sampulili'
                        )
print(x)
